server:
  port: 9990

# 内嵌聊天服务端口
im:
  port: 9999

logging:
  level:
    root: info
spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:tcp://localhost:9092/~/test
